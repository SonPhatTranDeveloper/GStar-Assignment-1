[project]
name = "gstar-assignment-1"
version = "0.1.0"
description = "Gstar Assignment 1 with answers (hopefully)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.8.0",
]
